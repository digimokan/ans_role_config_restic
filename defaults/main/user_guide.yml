# if this var is not empty, create a restic "user guide" in this directory
# NOTE: specify absolute path from root, with leading and trailing '/'
restic_user_guide_dir: ''

# the user guide is customized for a specific S3-compatible storage provider
# currently-available customizations:
#   - wasabi
restic_user_guide_format: 'wasabi'

